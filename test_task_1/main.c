#include "main.h"

int main(void)
{



  _printf("Omar %c %s %%", 'W', "Ahmed");
  
  /* int iterator = 0;
 
  char mystring[2048];

  while (iterator <2048)
    {
      mystring[iterator] = '~';
      iterator++;
    }

  iterator = 0;
  while (iterator < 2048)
    {
      printf("%c\n", mystring[iterator]);
      iterator++;
      }*/
  return (0);
}
